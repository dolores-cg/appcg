import CartWidget from "./CartWidget"

const Nav = () => {
    return (
        <nav>
            <CartWidget/>
                <a href="#">link</a>
                <a href="#">link</a>
        </nav>
    )
}

export default Nav
